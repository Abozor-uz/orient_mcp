services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_PASSWORD: local-test-only
    ports:
      - "127.0.0.1:55479:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 3s
      retries: 15
